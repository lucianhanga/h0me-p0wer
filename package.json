{
  "name": "h0me-p0wer",
  "private": true,
  "version": "0.7.0",
  "description": "Dashboard for the Anker SOLIX Smart Meter Gen 2 (AE1X0) — live Modbus data + Anker cloud history",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -k -n server,web \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run build && npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
